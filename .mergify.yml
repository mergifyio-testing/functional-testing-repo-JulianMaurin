merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - label=queue
  name: Merge me
queue_rules:
- name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci
