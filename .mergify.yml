merge_queue:
  max_parallel_checks: 2
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20260306135654/test_batch_formation_success_failure_and_bisection/main
  - label=queue
  name: auto-queue
queue_rules:
- batch_max_wait_time: 0 s
  batch_size: 3
  merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
