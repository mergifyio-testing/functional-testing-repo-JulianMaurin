pull_request_rules:
- actions:
    queue: {}
  conditions:
  - label=queue
  name: Queue
queue_rules:
- merge_conditions:
  - check-success=continuous-integration/fake-ci
  name: default
  queue_conditions:
  - label=queue
