merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20260309075605/test_inplace_checks_failure_and_synchronize/main
  - label=queue
  name: auto-queue
queue_rules:
- name: default
  queue_conditions:
  - check-success=continuous-integration/fake-ci
  update_method: rebase
