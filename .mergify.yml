merge_queue:
  max_parallel_checks: 5
  mode: parallel
queue_rules:
- batch_max_wait_time: 0 s
  batch_size: 2
  merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  queue_conditions:
  - base=20260315102719/test_monorepo_scope_lifecycle/main
  - label=queue
scopes:
  source:
    files:
      backend:
        include:
        - backend/**
      frontend:
        include:
        - frontend/**
