merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20260319160859/test_inplace_fast_forward_merge_sequential/main
  - label=queue
  name: merge fast-forward
queue_rules:
- merge_method: fast-forward
  name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci
